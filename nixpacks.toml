[variables]
UV_PROJECT_ENVIRONMENT = "/opt/venv"

[phases.install]
dependsOn = ["setup"]
cmds = [
  "python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install uv==0.11.8 && uv sync --no-dev --frozen",
]
paths = ["/opt/venv/bin"]
cacheDirectories = ["/root/.cache/uv"]
