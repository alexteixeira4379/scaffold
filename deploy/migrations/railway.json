{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run alembic -c alembic.core.ini upgrade head && uv run python -m scaffold.messaging.sync",
    "restartPolicyType": "NEVER",
    "healthcheckPath": null
  }
}
